# Committed placeholder defaults — do not put real values here.
# Override per-environment in client/.env.local (gitignored, agent-managed before each build).

ENDPOINT="https://your-instance.example.com/"
MODULE="/Monolith"
# APP=   — set in .env.local (per-env, required before building)
